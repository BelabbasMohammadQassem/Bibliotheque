{% extends 'base.html.twig' %}

{% block title %}Edit Book{% endblock %}

{% block body %}
<div class="container my-5">
    <h1 class="text-center mb-4">Edit Book</h1>

    <div class="card shadow p-4">
        {{ include('book_back/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <div class="d-flex justify-content-between align-items-center mt-4">
        <a href="{{ path('app_book_back_index') }}" class="btn btn-secondary">
            <i class="bi bi-arrow-left"></i> Back to List
        </a>

        {{ include('book_back/_delete_form.html.twig', {'attr': {'class': 'd-inline-block'}}) }}
    </div>
</div>
{% endblock %}
